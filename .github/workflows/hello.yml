name: Hello Action with My Email

on: [push]  # Triggers when you push code (easiest for testing)

jobs:
  say-hello:
    runs-on: ubuntu-latest  # Free Linux computer GitHub provides
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4  # Grabs your repo files
      
      - name: 22f1000821@ds.study.iitm.ac.in  # Your required step name!
        run: echo "Hello, world! Action ran successfully."  # Simple command
